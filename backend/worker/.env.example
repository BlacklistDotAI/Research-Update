# Worker Configuration

# Server API URL (required)
SERVER_URL=http://localhost:8000

# Worker authentication token (required)
# Get this from the admin panel after registering a worker
WORKER_TOKEN=your_worker_jwt_token_here

# Worker identification (optional, defaults to hostname)
WORKER_ID=worker-01

# Polling interval in seconds (optional, default: 5)
POLL_INTERVAL=5

# HTTP request timeout in seconds (optional, default: 30)
REQUEST_TIMEOUT=30
